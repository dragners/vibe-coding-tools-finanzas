import React from "react";

export default function App() {
  return (
    <div style={{ display: "grid", placeItems: "center", minHeight: "100vh" }}>
      <div style={{ maxWidth: 720, padding: 24, background: "white", borderRadius: 16, boxShadow: "0 1px 3px rgba(0,0,0,.1)" }}>
        <h1 style={{ marginTop: 0 }}>Herramienta ejemplo</h1>
        <p>Plantilla mínima para nuevas apps con Vite + React + TypeScript.</p>
        <p><a href="/">← Volver al índice</a></p>
      </div>
    </div>
  );
}
